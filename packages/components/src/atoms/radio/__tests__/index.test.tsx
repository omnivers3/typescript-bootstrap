import * as React from 'react';
import { Radio } from '..';

describe('components / radio', () => {
  it('checks out', () => {
    const actual = <Radio />;

    expect(actual).toEqual(actual);
  });
});
